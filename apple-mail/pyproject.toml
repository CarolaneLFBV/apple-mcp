[project]
name = "apple-mail-mcp"
version = "0.1.0"
description = "MCP server for Apple Mail (Mail.app) on macOS — read, search and organize email via AppleScript. No send capability."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [{ name = "Carolane Lefebvre" }]
keywords = ["mcp", "apple", "mail", "macos", "applescript", "claude", "model-context-protocol"]
classifiers = [
    "Environment :: MacOS X",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: MacOS :: MacOS X",
    "Programming Language :: Python :: 3",
    "Topic :: Communications :: Email",
]
dependencies = [
    "mcp[cli]>=1.2.0",
]

[project.urls]
Homepage = "https://github.com/CarolaneLFBV/apple-mcp"
Repository = "https://github.com/CarolaneLFBV/apple-mcp"
Issues = "https://github.com/CarolaneLFBV/apple-mcp/issues"

[project.scripts]
apple-mail-mcp = "apple_mail_mcp.server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/apple_mail_mcp"]

[dependency-groups]
dev = [
    "pytest>=8.0",
]
